language: c++
sudo: false

os:
    - osx
    - linux

osx_image: xcode7.3

compiler:
    - gcc
    - clang

script:
    - ./build_dependencies 
    - ./configure --with-mpir=${HOME}/local --with-mpfr=${HOME}/local --with-gmp=${HOME}/local --with-flint=${HOME}/local
    - make

